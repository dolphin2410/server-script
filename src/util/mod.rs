/// The `progress_bar` module is for notifying users about the current download state
pub mod progress_bar;

/// The `java_util` module is for checking if required java components are installed in the users' computers.
pub mod java_util;

/// The `runner_util` module handles optimized Aikar's settings for servers.
pub mod runner_util;

/// The `logger` module handles colored printing
pub mod logger;

/// The `paper_api` module is for converting versions and builds to a static url.
pub mod paper_api;